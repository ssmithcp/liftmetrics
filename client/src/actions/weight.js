// import api from '../util/api'

// import { ADD_WEIGHTS, ADD_WEIGHT, DELETE_WEIGHT, CLEAR_WEIGHTS } from '../reducers/weight'
import { alertOnAPIError } from './alert'


export const getProfile = () => dispatch => (
  alertOnAPIError(async () => {

  }, dispatch)
)