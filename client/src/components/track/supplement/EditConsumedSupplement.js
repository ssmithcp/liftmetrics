const EditConsumedSupplement = (props) => {
  const { match: { params: { id } } } = props;
  return <p>Edit consumed supplement page coming soon! (would be editing { id })</p>;
}

export default EditConsumedSupplement